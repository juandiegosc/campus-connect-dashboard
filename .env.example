# URL del API Gateway al que el dev server (Vite) hace proxy.
# El frontend SIEMPRE habla con el Gateway, nunca con los microservicios directo.
#
# Local con `dotnet run`:        http://localhost:5287
# Docker (--profile services):   http://localhost:8080
VITE_GATEWAY_URL=http://localhost:5287
